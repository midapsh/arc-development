export interface IRouteValueToTabItem {
  urlLocation: string;
  label: string;
}

export interface IRouteValueToMenuItem {
  urlLocation: string;
  label: string;
}

export interface IRouteValueToListItem {
  urlLocation: string;
  label: string;
}
